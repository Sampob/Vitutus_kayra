<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vitutuskyr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fiiliskayra_round_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.VitutusKäyrä">
        <activity android:name=".Notedetails"
            android:parentActivityName=".Muistiinpanot"/>
        <activity android:name=".Muistiinpanot"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".Kayra"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>